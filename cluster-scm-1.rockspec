package = 'cluster'
version = 'scm-1'
source  = {
    url = '/dev/null',
}
dependencies = {
    'lua >= 5.1',
    'checks >= 2.0',
    'lulpeg >= 0.1',
    'errors >= 1.0',
    'vshard >= 0.1',
    'membership >= 2.1',
}

build = {
    type = 'none';
}
