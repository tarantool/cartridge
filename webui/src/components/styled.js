import styled from 'react-emotion';

export const Title = styled.div(`
  font-size: 22px;
  font-family: 'Roboto';
  font-weight: 100;
  color: #61000D;
  display: block;
  text-transform: uppercase;
`)
